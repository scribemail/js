# pnpm reads build-script approval from here (no longer from package.json's "pnpm" field).
# esbuild ships a native binary via a postinstall script, so it must be allowed to build.
onlyBuiltDependencies:
  - esbuild
