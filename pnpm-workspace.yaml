# Approve dependency build scripts (pnpm >= 10.26; this is what `pnpm approve-builds` writes).
# esbuild compiles a native platform binary in its postinstall, so it must be allowed to build.
allowBuilds:
  esbuild: true
